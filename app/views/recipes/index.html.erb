<h1>Searching for: <%= @search_recipe %></h1>
<p>Powered By Food2Fork.com</p>
<% if @recipes.nil? || @recipes ==[] %>
  <p><h2>No recipes found... Sorry!</h2></p>
<%else%>
  <table border = "1">
    <tr>
      <th>Image</th>
      <th>Name</th>
      <th>Social rank</th>
    </tr>
    <% @recipes.each do |recipe| %>
      <tr class= <%= cycle('even', 'odd')%>>
        <td><%= link_to(image_tag(recipe['image_url'], size: "100x100"), recipe["f2f_url"]) %></td>
        <td><%= link_to( sanitize(recipe['title']), recipe["f2f_url"]) %></td>
        <td><%= recipe['social_rank'] %></td>
      </tr>
    <% end %>
  </table>
<%end%>
